# CountAway does not currently require project-specific R8 keep rules.
# Android framework entry points declared in the manifest and resources are
# handled by the Android build tools. Add narrowly scoped rules here only if a
# future dependency or reflective API requires them.
